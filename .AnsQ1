/*
    If we list all the natural numbers below 10 that are multipler of 3 or 5
    we get 3,5,6 and 9.The sum of these multipler is 23.
    Find the sum of all the multiples of 3 or 5 below 1000.
 */
package javaapplication4;

public class JavaApplication4 {

    public static void main(String[] args) {
        
        int sum = 0;
        for(int i = 0;i<=999;i++)
            if(i%3==0 || i%5==0){
            System.out.printf("\nNumber = %d",i);
            sum+=i;
        }
        System.out.printf("\n\nSum = %d",sum);
    }
    
}
